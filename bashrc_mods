# Set default command line editor to vim if it is installed.
if hash vim 2>/dev/null; then
	export VISUAL=vim
	export EDITOR="$VISUAL"
fi

# Add some safety prompts to potentially dangerous commands.
alias cp="cp -i"
